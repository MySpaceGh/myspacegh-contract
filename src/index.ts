/**
 * @myspacegh/contract — the single source of truth for cross-app domain enums.
 *
 * Consumed by the web app and the mobile app so their TypeScript enums can never
 * drift from the backend. Mirrors backend/app/Enums/*.php — see ./enums.ts.
 */
export * from './enums';
